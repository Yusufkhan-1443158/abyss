# REQUIRED — NASA EarthData credentials
EARTHDATA_USERNAME=your_username
EARTHDATA_PASSWORD=your_password

# Server port (Railway sets automatically)
PORT=8080

# Optional — data storage directory
DATA_DIR=/tmp/bathymetry_data
